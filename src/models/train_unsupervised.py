"""Entrenamiento del enfoque no supervisado de detección de anomalías (sin usar la etiqueta)."""

import pandas as pd


def train_unsupervised_model(X: pd.DataFrame):
    """Entrena un modelo de detección de anomalías sobre las features de pagos."""
    raise NotImplementedError
