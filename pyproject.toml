[project]
name = "cms-fraud-detection"
version = "0.1.0"
description = "Detección de fraude en CMS Open Payments cruzado con la lista LEIE: comparación de modelado supervisado vs. detección de anomalías no supervisada."
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
